const menuIcon = document.querySelector('.hamburger-menu');
const navbar = document.querySelector('.navigation-menu');
const bgmenu = document.querySelector('')
menuIcon.addEventListener('click', () =>{
    navbar.classList.toggle('change')
});